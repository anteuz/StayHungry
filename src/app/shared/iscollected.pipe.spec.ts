import {IsCollectedPipe} from './iscollected.pipe';


describe('IscollectedPipe', () => {
  it('create an instance', () => {
    const pipe = new IsCollectedPipe();
    expect(pipe).toBeTruthy();
  });
});
